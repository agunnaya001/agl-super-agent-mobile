# Placeholder defaults so the app boots before credentials are supplied.
# Real secrets are delivered by the platform to /run/base44/app.env (last env_file entry wins).
GEMINI_API_KEY=MY_GEMINI_API_KEY
